# CLAUDE.md

## Git Identity

When making git commits, always use the user's git identity. Never add Co-Authored-By lines or any trailer to commits. Run git config user.name 'albedimaria' and git config user.email 'alberto.dimaria@mail.polimi.it' before any commit if not already set.
